% ======================================================================================================================
% GENERATED FROM 1_code/31-OnlineAppendix.qmd -- DO NOT EDIT THIS FILE. Rendered $date$.
% Edit the .qmd and render it again: an edit made here is lost at the next render.
% ======================================================================================================================
%
% A FRAGMENT, NOT A DOCUMENT: no preamble and no \begin{document}. The paper's online-appendix file sets the lettered
% numbering and inputs this file. Every definition below is \providecommand, so one the paper already made wins.
%
% Where 30's deployed exhibits (Tables/, Notes/, Figures/) and the paper's static tables sit in the Overleaf project.
\providecommand{\oaexhibits}{exhibits/}
\providecommand{\oastatic}{tables/}
% For the static tables only: shrunk to the line when wider, never enlarged. 30's tables carry their own frame.
\providecommand{\oafit}[1]{\resizebox{\ifdim\width>\linewidth\linewidth\else\width\fi}{!}{#1}}
% An exhibit 30 has not built yet: a framed line in its place, so the paper still compiles.
\providecommand{\oamissing}[1]{\fbox{\parbox{0.9\linewidth}{\centering\footnotesize Exhibit \texttt{#1} is not built yet.}}}
% Pandoc writes \tightlist into compact lists and \pandocbounded around images; the paper's preamble defines neither.
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\providecommand{\pandocbounded}[1]{#1}
%
$body$
